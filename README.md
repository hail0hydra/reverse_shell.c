# Reverse_Shell.c

- This repositroy contains `reverse_shell` and `bind_shell`
implementation in __C-language__

- The implentation is done using standard `syscalls` provided by libc.

- This is only working for UNIX based environments.

- compile any program and run a netcat listener/connection to see this
work.
